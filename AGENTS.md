# AGENTS.md

本文件适用于整个项目。处理本项目时，先遵守这里的规则。

## 和用户沟通

- 全程使用中文。代码注释和新增文档也使用中文。
- 用户是新手。先说结论，再用简单的话解释原因。
- 少用专业缩写。必须使用时，第一次要解释，例如：
  - CAGR：年化收益率。
  - OOS：样本外，也就是没有拿来选参数的测试时间段。
  - Sharpe：衡量收益和波动是否匹配的指标，通常越高越好。
  - manifest：固定本轮研究用了哪些股票和哪一版数据的清单。
  - 哈希：文件的“指纹”；哈希相同，说明输入内容没有变化。
  - 未来函数：在历史某一天做决定时，偷用了当时还不知道的数据。
- 命令使用 Windows PowerShell 写法，并说明命令会做什么。
- 遇到失败时，说清楚“哪里失败、会影响什么、下一步怎么处理”，不要只贴报错。
- 历史回测不是未来收益保证，也不是买卖建议。不要使用“稳赚”“一定复现”等说法。

## 开始工作前

1. 先读 `README.md`，了解项目怎么运行。
2. 需要知道当前最优策略时，读 `docs/STRATEGY_STATUS.md`。这里是当前结果的唯一权威说明。
3. 需要继续策略实验时，读 `docs/EXPLORATION_LOG.md`，避免重复已经做过的实验。
4. 改候选池、数据截止日或输入文件时，先读 `docs/UNIVERSE_MANIFEST.md`。
5. 运行 `git status --short`，分清已有修改和本次修改。保留用户已有内容。

## 项目目标

- 在规则简单、能解释、能复现的前提下，研究更好的候选池和策略。
- 优先提高交易成本后的长期年化收益，同时检查最大回撤和不同时间段的稳定性。
- 所有结论必须来自固定输入、可重复运行的脚本和可审计的结果文件。
- 模型只做研究，不连接券商，不读取账户，不自动下单。

## 不能破坏的回测边界

- 信号只能使用信号日当天或更早已经知道的数据。
- 买卖默认在信号日之后的下一可用交易日执行，不能用信号日收盘价假装成交。
- 候选池必须按每个历史信号日重新计算，不能把今天筛出的股票名单倒推到过去。
- 分红、送股和转股按真实除权日处理；买入前或买入当天的分红不能记入持仓收益。
- 动量只能读取信号日前的价格。缺少完整回看数据时，不能用更短时间偷偷替代。
- 默认使用 `data/universe_manifest.json`、`data/rebalance_dates_monthly.json` 和 `data/backtest_cache/` 中的冻结输入。
- 生成或刷新输入时必须显式写出截止日，并重新生成 manifest、日期文件和哈希。
- 不要静默改用复权价格、在线最新数据、不同候选集合或旧实验的隐藏默认参数。
- 当前冻结集合可能缺少退市股票，存在生存者偏差。报告结果时必须保留这个限制。

## 每轮策略探索

1. 先写清楚本轮要验证的问题和基线。优先一次只改一个规则；需要组合时只测试少量参数组合。
2. 基线和挑战者必须使用同一份 manifest、日期文件、数据截止日、价格口径和费用口径。
3. 至少记录以下结果：年化收益率、最大回撤、Sharpe、交易次数、滚动 36/48 月最差结果，以及连续样本外结果。
4. 对看起来更好的挑战者，再检查不同起点、较高交易成本和信息延迟。不能只看全样本最高年化收益率。
5. 策略越简单越好。新增规则只有在多个检查中稳定改善，且能清楚解释时，才可以进入当前主策略。
6. 每轮保存可重复运行的 `scripts/roundN_*.py` 和对应的 `data/roundN_*.json`。结果文件要记录输入哈希、截止日、规则和限制。
7. 每轮结束后都要更新：
   - `docs/EXPLORATION_LOG.md`：记录做了什么、数据、结果、限制和采用或淘汰原因。
   - `docs/STRATEGY_STATUS.md`：更新当前最优策略；如果没有变化，也要明确写“主策略不变”。
   - `README.md`：只更新简短摘要、复现入口和实验索引。
8. 向用户汇报本轮结果、当前最优策略、是否切换主策略，以及下一轮准备验证什么。

## 修改代码时

- 优先沿用现有函数和实验脚本的写法，避免为了一个小实验增加复杂框架。
- 修复错误或新增规则时，补充能证明时间边界和默认行为的测试。
- 不删除历史实验结果。发现旧结果错误时，在文档中标记失效原因，并生成新的可审计结果。
- 不执行 `git reset --hard`、`git clean` 或会覆盖已有修改的命令。
- 不提交大体积临时文件、浏览器缓存、虚拟环境或密钥。

## 完成前检查

先运行与改动直接相关的测试，再运行完整检查：

```powershell
pytest -q
python -m compileall -q scripts tests
git diff --check
```

还要确认：

- 新增 JSON 可以正常解析。
- 当前基线可以用 `README.md` 中的命令复现。
- 结果里的 manifest 哈希、日期哈希和截止日与本轮输入一致。
- 没有遗留仍在运行的实验进程。
- 文档中的数字和生成的 JSON 一致。

如果某项检查没有运行或没有通过，必须在交付说明中明确写出。
