# 策略状态（权威口径）

> 更新时间：2026-08-31。本文只把固定且带指纹的输入上的结果作为当前结果；旧报告中的指标如果没有满足下列输入约束，均视为历史/失效结果。逐轮实验详见 [EXPLORATION_LOG.md](EXPLORATION_LOG.md)。

## 当前研究候选

**名称：** 月度高息动量策略（动态池）

**回测区间：** 2016-01-29 至 2026-08-25，共 128 个信号点；信号在月末收盘形成，默认下一可用交易日收盘执行（`execution_lag_days=1`）。

### 指标

| 指标 | 当前候选 |
|---|---:|
| CAGR | **41.38%** |
| 最大回撤 | 28.06% |
| Sharpe | 1.217 |
| 期末 NAV（10 万元起） | 3,876,245.51 |
| 交易次数 | 75 |
| 期末现金 | 569.51 |
| 滚动 36 月最差 CAGR | 9.58% |
| 滚动 48 月最差 CAGR | 17.29% |

以上数字来自冻结的 `data/current_best.json`，并由 V1 指纹校验和第 30、31 轮
同输入基线复核；不是收益保证。`current_best.json` 还包含逐信号日
`pool_provenance`，可复核动态池数量、候选代码哈希和执行日期。

### 参数

| 参数 | 值 |
|---|---:|
| `pool_mode` | `dynamic` |
| `pool_min_consecutive_years` | 3 |
| `pool_switch_month` | 7 月 |
| `entry_yield` | 7.5% |
| `hold_yield` | 5.5% |
| `momentum_months` | 4 |
| `momentum_threshold` | 0.85 |
| `max_holdings` | 2 |
| `rebalance_threshold` | 2.0 |
| `max_position_pct` | 1.0（不额外限仓） |
| `reinvest_cash_reserve` | 0 |
| `stop_loss_pct` | 0（禁用） |
| `dividend_information_lag_days` | 0（第 17 轮作为压力参数） |
| `frequency` | `monthly` |

### 连续样本外与分段结果

样本外从完整账本的 NAV 序列切片，**不重新初始化现金和持仓**；这是本项目统一的 OOS 口径。

| 窗口 | CAGR | 最大回撤 |
|---|---:|---:|
| 2021-01 至 2026-08 | 43.62% | 19.43% |
| 2023-01 至 2026-08 | 24.74% | 13.76% |
| 连续 OOS：2025-01 至 2026-08 | 32.05% | 13.76% |
| 2016-01 至 2018-12 | 32.34% | 28.06% |
| 2019-01 至 2021-12 | 66.26% | 19.43% |
| 2022-01 至 2024-12 | 45.50% | 20.62% |
| 非重叠块：2025-01 至 2026-08 | 32.05% | 13.76% |

### 低回撤对照

`momentum_months=2`、`momentum_threshold=0.95`、保留现金 5,000 元的版本：CAGR 36.89%、最大回撤 23.70%、Sharpe 1.096，滚动 36 月最差 5.51%。它的复杂度和绝对收益都不如当前候选，因此暂不作为主配置。

### V5 独立影子重建

V5 是独立影子策略，不替换 V1，也不回写 V1 账本。`data/round32_v5_rebuild.json`
使用 100 万元历史初始资金；前向账户使用独立 10 万元。当前冻结重建结果为：

| 指标 | 正常费用 | 三倍费用 |
|---|---:|---:|
| CAGR | 12.88% | 12.49% |
| 最大回撤 | 21.03% | 21.30% |
| Sharpe | 0.798 | 0.778 |
| 交易次数 | 198 | 200 |
| 滚动 36 月最差 CAGR | 1.63% | 1.25% |
| 滚动 48 月最差 CAGR | 3.83% | 3.51% |
| 2021 起连续样本外 CAGR | 14.55% | 14.11% |
| 2023 起连续样本外 CAGR | 5.91% | 5.55% |

附件报告的 16.40% 不作为本项目结果。当前重建使用新浪前复权因子与冻结不复权
缓存重建动量/波动率，股息率和成交继续使用不复权价格；点时 EPS、官方历史行业
分类和 H00922 单独冻结。最终 V5 输入指纹为
`2d19f3944d66b6e120357fabaac6508c595c3368ea2e858f8ea7839b26c3a6d2`。
附件 `2015-12-31` 的阀门目标为 0.615，已按锁定下行半偏差公式反算验证。
冻结 210 只覆盖集合仍有幸存者偏差。

### 多资产风险预算 V2.2（全球版影子）

V2.2 是独立四 ETF 影子策略，不替换高息动量 V1。规则为 126 日正动量门控、
63 日逆波动率风险预算、10% 组合波动目标和国债 ETF 防守仓；月末收盘形成信号，
下一真实交易日开盘模拟成交。`data/round33_ma_v22_rebuild.json` 的结果为：

| 指标 | 正常费用 | 三倍费用 |
|---|---:|---:|
| CAGR | 13.12% | 12.02% |
| 最大回撤 | 13.58% | 13.65% |
| Sharpe | 1.040 | 0.947 |
| 交易次数 | 418 | 418 |
| 滚动 36 月最差 CAGR | 4.32% | 2.94% |
| 滚动 48 月最差 CAGR | 6.68% | 5.68% |
| 2021 起连续样本外 CAGR | 11.30% | 10.10% |
| 2023 起连续样本外 CAGR | 17.76% | 16.76% |

正常费用下平均权重为沪深300 19.73%、黄金 30.89%、纳指100 28.62%、国债
20.76%。冻结输入截至 2026-08-28，共 3,182 个共同交易日和 24 条基金分红，
指纹为 `e1a9236ccf872b41ac4fb4a2eca22fd5adb64ef6857ab1c0aa3f78d3cc07c994`。
参考包缺少自检 CSV，因此不声明逐点完全复现；历史结果不代表未来收益。


## 第 8–33 轮探索结论

完整证据和每轮脚本见 [EXPLORATION_LOG.md](EXPLORATION_LOG.md)。汇总如下：

| 轮次 | 主要问题 | 结果 | 当前处理 |
|---|---|---|---|
| 8 | 连续分红年限、入场线、持仓数 | 3 年、7.5%、2 只综合最好 | 保留 |
| 9 | 排序、动量周期、再平衡、高息上限及数据质量 | 未发现可采纳改进；缓存无重复/负分红，价格有除权断点 | 保留基线并记录警告 |
| 10 | `hold_yield` 极窄邻域 | 5.6% 全样本 CAGR 42.90%，形成平台候选 | 待独立窗口验证 |
| 11 | 平台、池门槛和简单控制变量复核 | 5.575%–5.675% 结果相同；3 年池门槛仍最好 | 5.6% 仅作挑战者 |
| 12 | 真实重置窗口、成本压力、provenance | 5.6% 的优势依赖起点；高成本时反转；审计字段补齐 | 主配置继续 5.5% |
| 13 | 高息上限、严格 warm-up 窗口 | 上限无改善；5.6% 多数窗口略优但 2022 年反转 | 主配置继续 5.5% |
| 14 | 持仓动量退出、候选池连续性、基准 | 退出和替代池均未改善；价格基准仅作相对参照 | 保持基线 |
| 15 | DPS 不下降过滤、无动量/无复投控制 | 质量过滤降低收益；控制 CAGR 8.65% | 保持基线 |
| 16 | 个股最新已知池、复投贡献、稀疏频率 | 替代池和双月/三月均未同时改善；复投贡献显著 | 保持月度基线 |
| 17 | ex_date 信息延迟、收益率口径、联合网格 | 0 天最优但延迟敏感；5.6% 峰值在重置/成本下不稳 | 保持 5.5% |
| 18 | 候选池确认月份、延迟下邻域、训练/测试选参 | 7 月优于 5/6 月且与 8 月全样本等价；延迟和训练赢家未改善基线 | 保持 7 月与 5.5% |
| 19 | 多周期动量、集中度约束、分红时点字段 | 单 4 月动量最优；单行业上限降级；公告/登记字段缺失 | 保持当前组合 |
| 20 | 仓位上限/准备金、实际退出确认、分红缓存审计 | 额外限仓和准备金降低 CAGR；5.5% 单次确认综合最好；缓存 0 异常 | 保持当前组合 |
| 21 | 亏损持仓退出线、5.3% 挑战者独立区块 | 4.5%–5.5% 亏损线交易完全相同；5.3% 优势不跨越独立区块 | 保持 `hold_yield=5.5%` |
| 22 | 连续分红年限、再平衡阈值 | 3 年优于 4/5 年；2.0 与 2.5 逐笔等价，1.5 增加交易且收益下降 | 保持 3 年、2.0 |
| 23 | 最多持仓数量 | 2 只正常成本 CAGR 41.38%；1 只明显降级，3 只仅在三倍成本压力下局部占优 | 保持 2 只 |
| 24 | 候选池排序依据 | 按真实股息率显著优于按动量排序，后者滚动和近期 OOS 转负 | 保持 `rank_by=yield` |
| 25 | 幸存者偏差审计（退市股票） | 东财 API 对 271 只退市股均不返回分红数据，确认数据源覆盖缺口 | 幸存者偏差不可量化，保留警告 |
| 26 | 日频 NAV 审计 | 月度口径低估真实日频最大回撤 4.38pp（28.06%→32.44%） | 主策略不变，标注日频回撤参考值 |
| 27 | 动量回看周期 3/4/5/6 个月 | 4 个月全面碾压其他周期（CAGR 高出 9–17pp） | 强力确认 `momentum_months=4` |
| 28 | 波动率调整排序 | yield_vol CAGR 32.92% 低于 yield 41.38%，回撤相同 | 明确淘汰，保持 `rank_by=yield` |
| 29 | 收益归因分析 | 资本利得占 80.5%，分红占 19.5%；前 3 只占盈亏 69%；胜率 77.8%（14/18） | 主策略不变，增加收益来源透明度 |
| 30 | 剔除头部个股/行业、三倍费用和含分红基准 | 剔除最大盈利股后 CAGR 26.55%、最大回撤 47.45%；材料化工和汽车产业链代理簇剔除后滚动 48 月最差转负 | 确认明显集中度与周期依赖，冻结观察、不调参 |
| 31 | 持仓上限 2–10 一次性扫描 | 2 只正常成本综合最好；3 只三倍费用 CAGR 最高；8–10 只回撤较低但收益和滚动最差值明显下降 | 主策略保持 2 只；3/4 只按用户要求进入 V2/V3 影子观察 |
| 32 | 附件 V5 的可审计重建和前向接入 | 当前冻结数据重建 CAGR 12.88%，低于附件 16.40%；四策略隔离演练通过 | V1 不变；V5 仅作独立影子观察 |
| 33 | 多资产风险预算 V2.2 参考代码复算和前向接入 | CAGR 13.12%、最大回撤 13.58%、Sharpe 1.040；五策略隔离演练通过 | V1 不变；V2.2 仅作独立影子观察 |

因此，当前最优的可复现主策略仍是本页顶部的 `hold_yield=5.5%` 版本；
5.6% 的历史平台不被写成普遍或未来必然改进。

第 22–31 轮继续使用同一冻结 manifest、日期文件和不复权收盘价。连续分红
3 年、最多 2 只、按真实股息率排序的组合，在正常成本下仍为 CAGR 41.38%、
最大回撤 28.06%、Sharpe 1.217；三倍费用和独立重置窗口没有形成足以替换它的
单一规则。第 23 轮的 3 只持仓在三倍费用下 CAGR 40.29%，但正常费用和交易次数
均不占优；第 24 轮的动量排序 CAGR 28.88%、最大回撤 47.45%，明确淘汰。
第 25–29 轮结论：退市股分红数据覆盖缺口不可量化（25，已由后续历史池临时
回放进一步验证风险）；月度回撤低估 4.38pp（26）；
4 个月动量全面碾压其他周期（27）；波动率调整排序降低收益且未改善回撤（28）；
收益归因增加了来源与集中度透明度，但不改变策略规则（29）。
第 29 轮收益归因：资本利得占总盈亏 80.5%、分红收入占 19.5%；前 3 只
持仓贡献 69% 的总盈亏；18 只持仓中 14 只盈利（胜率 77.8%），平均持仓
约 353 天。收益集中度较高但分布在多只股票，不改变策略规则。

第 31 轮是用户明确要求的一次性持仓上限扫描，只改变 `max_holdings=2..10`。
2 只在正常成本 CAGR、Sharpe、滚动 36 月最差和 2023 起连续样本外表现上领先；
3 只正常成本 CAGR 仅低 0.70 个百分点，并在三倍费用下以 40.29% 领先，但
交易次数从 75 增至 93。8–10 只的月频最大回撤约 22.8%，代价是 CAGR 降至
约 26% 且滚动 36 月最差接近 0%。没有版本形成足以替换 V1 的联合优势。

## V1 冻结与第 30 轮脆弱性

V1 已冻结在提交 `c7d128ff0bc1b4b21c60bc7c6e2894dabf513fae`。固定输入
见 `data/v1_freeze.json`，可运行 `python scripts/verify_v1_freeze.py` 校验。
本轮不调整任何参数，只改变审计时排除的股票集合。

| 对照 | CAGR | 最大回撤 | 滚动 36 月最差 CAGR | 滚动 48 月最差 CAGR |
|---|---:|---:|---:|---:|
| 冻结 V1 | 41.38% | 28.06% | 9.58% | 17.29% |
| 剔除最大盈利股 `601088` | 26.55% | 47.45% | -6.57% | 4.45% |
| 同时剔除前三大盈利股 | 26.55% | 47.45% | -6.57% | 4.45% |
| 剔除材料化工代理簇 | 22.15% | 43.92% | -9.01% | -4.66% |
| 剔除汽车产业链代理簇 | 24.58% | 47.45% | -8.96% | -5.31% |

510880 红利 ETF 含分红可交易基准在同观察区间的 CAGR 为 8.52%、最大回撤
19.90%、滚动 48 月最差 CAGR 0.73%，累计模拟分红现金 68,337.70 元。它的
收益低于 V1，但回撤也更小。以上审计说明 V1 的历史优势对头部个股和周期行业
依赖明显。第 31 轮一次性持仓扫描已经完成，但没有改变这个判断；当前决策仍是
继续冻结 V1 并做前向观察。

## 人工数据质量门禁历史回放

历史股票主数据覆盖 5,549 只沪深股票。门禁只处理曾进入冻结 V1 信号前候选
路径的股票，并在回放前固定规则：分红事件必须能与独立来源逐项闭合，价格必须
由独立来源确认；排除逻辑不读取回测盈亏。结果状态为
`complete_with_exclusions`：

| 门禁范围 | 放行 | 排除 | 说明 |
|---|---:|---:|---|
| 在市分红 | 190 | 15 | 205 只决策相关股票均已处理，15 只事件无法一一闭合 |
| 在市价格 | 190 | 0 | 499,639 行；873 个争议日期均由第三源形成多数 |
| 退市分红 | 47 | 91 | 另有 117 只按事先规则不属于连续三年分红候选 |
| 合并过滤池 | **237** | — | 190 只在市股票 + 47 只退市股票 |

同一派生缓存上的原 210 只控制组精确复现冻结 V1：CAGR 41.38%、最大回撤
28.06%、75 次交易。237 只过滤历史池的两个退市处置边界结果相同，因为这次
放行路径没有实际持有至退市的事件：

| 指标 | 人工门禁回放 |
|---|---:|
| CAGR | **11.29%** |
| 最大回撤 | **55.46%** |
| Sharpe | 0.416 |
| 期末 NAV（10 万元起） | 309,576.04 |
| 交易次数 | 28 |

这个结果不能称为全市场无偏回测。人工门禁排除了证据无法闭合的股票，仍可能
存在数据可得性偏差；它也不替换冻结 V1 的可复现基线。但它显著削弱了对
41.38% 历史 CAGR 稳定性的信心，因此当前决策仍是停止调参并进入模拟盘前向
观察。早期 `data/historical_v1_provisional.json` 只保留作审计历史，当前结果
以 `data/historical_v1_filtered.json` 和 `data/historical_filtered_manifest.json`
为准。

## 模拟盘前向观察

模拟盘只写 `data/forward/`，不会覆盖冻结回测输入。GitHub Actions 每个工作日
北京时间 18:30 首次运行，此后每小时做同日幂等重试至 22:30；每次先校验五策略
冻结合同、真实交易日和 510300 当日收盘价。每个真实交易日更新公开业绩，
自动任务以北京时间 18:00 为收盘数据可用边界；延迟跨日且在 18:00 前启动时
仍处理前一日，避免在凌晨请求尚未产生的新交易日收盘价。
只有当月最后真实交易日才生成信号，且只允许在下一真实交易日记录模拟执行。
月度事件错过日期会失败，不允许补写。任务失败时创建或更新固定 GitHub Issue，
恢复后自动关闭；公开页面超过一个工作日未更新时显示数据滞后提示。

- 第一期信号：五策略均已在 2026-08-31 收盘后生成信号。V5 从 169 只动态
  候选中选出 `600066`、`000651`，风险阀门目标为 90.22%。
- 第一期模拟执行：若 2026-09-01 正常开市，高息动量策略在当日收盘模拟执行，
  多资产风险预算 V2.2 在当日开盘模拟成交并按收盘估值。
- 当前账本：高息动量 V1 和四个影子策略均有 1 条 `2026-08` 信号，尚无执行事件；
  五个账户各持有 10 万元现金，收益为 0%。
- 当前公开业绩：截至 2026-08-31，五策略均显示“已生成首期信号，等待下一
  交易日执行”，健康状态全部正常；510300 等待 V1 首笔模拟成交日再同步建仓。
- 公开页面：<https://ainioayi.github.io/dividend-strategy-quarterly/>。页面读取
  `site/performance.json`，同图展示六条收益曲线，并可切换五策略查看各自累计
  盈亏、持仓、交易明细和账本指纹。除高息动量 V1 外均标记为影子观察。
- 行情口径：510300 与 V1 首笔模拟成交同日用 10 万元建仓，使用场内未复权
  收盘价、整手、交易费用和现金分红复投；此前双方均按现金 0% 计。新浪与腾讯
  同语义日线交叉核对，缺少当日收盘价时停止发布。
- 资金口径：V1 对策略账户按 100% 目标投入，额外现金保留为 0；仍遵守 A 股
  100 股整数手和交易费用约束，无法再买一手的现金尾差保留在账户中。
- 观察周期：至少 6 个月、目标 12 个月；期间保持五策略规则不变。高息动量 V1/V2/V3
  除 `max_holdings=2/3/4` 外，选股、择时、费用、分红和执行规则完全相同；每套
  独立 10 万元并按 100% 目标投入。V2/V3 只能写入 `data/forward/shadow/`，不能
  写 V1 账本，也不替代 V1 主策略。V5 独立使用六仓、双风险阀门、现金计息、
  波动率与行业门；多资产风险预算 V2.2 使用四 ETF 风险预算和次日开盘成交。
  任一影子策略失败时只更新自身健康状态。
- 上线前演练：`scripts/rehearse_forward_cycle.py` 使用 210 只正式前向缓存的隔离
  副本，以最近已知价补出 2026-08-31 和 2026-09-01，同时对五策略完整触发
  signal、execute 和 510300 同步建仓。演练失败会阻止 GitHub Actions 正式更新并
  进入失败告警；手动 `mode=rehearsal` 可以只跑演练且不操作正式告警。模拟价格
  只验证控制流，不代表未来行情或真实选股结果。
- 2026-08-31 本地全链路演练已通过：高息动量 V1/V2/V3 各自从 210 只缓存生成 169 只
  动态池、1 只模拟放行并执行 1 只持仓；V5 合成输入放行 6 只并执行 6 只持仓，
  风险阀门后投入 80.13%；多资产风险预算 V2.2 放行并执行 3 只 ETF，投入
  97.03%。演练只使用隔离目录，正式五本账本的 SHA256 在演练前后完全相同。

### 五策略上线边界

| 版本 | 最多持仓 | 身份 | 独立账本 | 历史定位 |
|---|---:|---|---|---|
| 高息动量 V1（2只正式） | 2 | 正式前向模拟 | `data/forward/monthly_v1.jsonl` | 当前主策略 |
| 高息动量 V2（3只影子） | 3 | 影子观察 | `data/forward/shadow/monthly_v2.jsonl` | 最接近 V1 的挑战者 |
| 高息动量 V3（4只影子） | 4 | 影子观察 | `data/forward/shadow/monthly_v3.jsonl` | 用户指定扩展对照 |
| 高息动量 V5（附件规则影子） | 6 | 独立影子观察 | `data/forward/shadow/monthly_v5.jsonl` | 附件 V5 的当前项目数据重建 |
| 多资产风险预算 V2.2（全球版影子） | 4 | 独立影子观察 | `data/forward/shadow/monthly_ma_v22.jsonl` | 四 ETF 参考代码规则复算 |

自动化先汇总五本账本的门禁，只要任一策略缺少当期动作就继续；随后五个策略
各自用 `auto` 模式决定写入或跳过，动作冲突时失败关闭。任一影子失败时，V1 账本
不会被影子代码改写；公开数据会保留“失败，未冒充更新成功”的健康状态，发布
完成后任务仍标记失败并触发告警。前向产物提交后会显式触发 GitHub Pages 部署，
避免 Actions 自身提交不能级联触发工作流。510300 只跟随 V1 首笔模拟成交日。

### 回测与实时模型分层

历史回测是纯股息率动态池实验：`scripts/backtest.py` 将 `entry_pr`、`hold_pr`、
`exit_pr` 设为 `999`，避免把实时财务门槛混入历史收益比较。实时季度路径则先由
`scripts/optimized_strategy.py` 施加 `pr_ceiling=1.2`、连续分红 8 年等硬门槛，
再由 `scripts/quarterly_strategy.py` 使用 `hold_pr=1.2`、`exit_pr=1.5`。因此
`config/strategy.json` 中的 `entry_pr=999` 只表达回测层的无 PR 限制，不能解释为
实时筛选没有 PR 门槛；两层结果必须分开引用和复现。

第 20–29 轮的完整表格、脚本和输入哈希见 [EXPLORATION_LOG.md](EXPLORATION_LOG.md)。
其中 30 天延迟代理下动量阈值 0.84 只产生局部缓解，不能替代当前口径；
训练期选出的参数在后续测试中也未稳定超过基线。第 14 轮新增的 `momentum_exit_threshold` 与
`momentum_exit_confirm_count` 仅作为默认关闭的研究参数；任何启用值都不
属于当前配置。第 15 轮的固定池/无动量/无复投结果也不是可交易总回报基准。
第 16 轮稀疏频率脚本已按日历跨度计算滚动窗口；第 17、18 轮 OOS 均为完整账本
连续切片，不等同于重新开户的独立样本外。第 18 轮新增 `pool_switch_month`
默认值为 7，切换月份只改变确认年度边界，不改变交易执行规则。
第 19 轮验证单 4 月动量优于多周期几何均值，`max_sector=1` 会显著降低收益；
当前缓存缺少公告日和登记日，暂不把抓取时点或计划日期当作历史可得信息。
第 20 轮的仓位上限和现金准备金虽然降低部分回撤，但没有改善收益、滚动窗口和
成本压力的联合目标；实际执行路径的软退出线是 `hold_yield`，`exit_yield` 目前
仅用于理论触发价审计。第 21 轮显示亏损持仓线 4.5%–5.5% 在冻结账本中完全
等价，5.3% 挑战者在近期窗口较强但在 2020–2022 独立区块明显落后，因此不切换。

## 策略流程

1. 在固定 manifest 覆盖的代码集合内，按每个信号日已经实施的逐笔分红筛选连续分红至少 3 年的动态池；7 月起切换到上一年度确认窗口。
2. 用截至信号日可见的后置 DPS 与不复权收盘价计算真实股息率，达到 7.5% 才允许新入场。
3. 新入场股票须满足当前价格/四个月前价格不低于 0.85；已有持仓不受该过滤影响。
4. 股息率低于 5.5% 时退出，按当前持仓规则执行；软退出默认一次月度信号确认，
   不使用止损。配置中的 `exit_yield` 当前只用于理论触发价审计。
5. 最多持有两只股票，超出等权目标 2 倍才削减，并计入佣金、印花税和过户费。
6. 分红按实际除权日逐笔入账，现金在下一次账本处理时按整数手再投资。

`dividend_information_lag_days=0` 是当前明确记录的点时可得口径。第 17 轮显示
加入 30/60/90 天保守延迟后收益和滚动稳定性明显下降；第 18 轮的小范围参数
测试没有消除这一敏感性。缓存缺少公告日、登记日，这些数值不能解释为真实信息
发布延迟。

## 固定输入与复现

| 项目 | 值 |
|---|---|
| 行情缓存 | `data/backtest_cache/`，不复权收盘价 |
| 行情主源 | `sina_stock_zh_a_daily`；刷新失败时使用东财在线接口 |
| 分红明细 | 东财 `RPT_SHAREBONUS_DET`，缓存逐笔除权日 |
| 覆盖代码数 | 210 |
| manifest 哈希 | `24de009d9bb60c857fc89e8f7510b93583b17f9abde50350ea63a6a5830a7409` |
| 日期文件 | `data/rebalance_dates_monthly.json`，128 点 |
| 日期哈希 | `f62fc22c2f2f972e3b29dea42e2a41202bfa620e702acc3c750e26f8c959ec3e` |
| 数据截止日 | 2026-08-25 |

复现命令：

```powershell
python scripts/backtest.py `
  --dynamic-pool `
  --manifest data/universe_manifest.json `
  --rebalance-dates data/rebalance_dates_monthly.json `
  --param entry_yield 7.5 `
  --param hold_yield 5.5 `
  --param momentum_months 4 `
  --param momentum_threshold 0.85 `
  --param reinvest_cash_reserve 0 `
  --json data/current_best.json
```

候选池覆盖文件不是事后精选名单：`manifest.rules.pool_mode=dynamic`，`pool_min_consecutive_years=3`；manifest 保留全量缓存代码，动态规则在每个历史日期重新计算。

## 第 7 轮探索（2026-08-26）

产物：`data/round7_local.json`。共 21 组，所有变体从同一份显式基线出发，
先运行完整账本，再从 NAV 序列切连续 OOS；不重新开户、不使用其它实验脚本
的隐藏默认值。

| 变体 | CAGR | 最大回撤 | Sharpe | 滚动 36 月最差 | OOS 2021 起 |
|---|---:|---:|---:|---:|---:|
| 4 个月 / 0.84 | 38.07% | 28.06% | 1.058 | 9.13% | 37.39% |
| **4 个月 / 0.85（当前）** | **41.38%** | 28.06% | **1.217** | **9.58%** | **43.62%** |
| 4 个月 / 0.86 | 38.92% | 28.06% | 1.161 | 9.07% | 38.98% |
| 4 个月 / 0.87 | 37.14% | 28.06% | 1.135 | 9.03% | 35.65% |

阈值 `0.84–0.87` 是局部平台，`0.85` 在全样本 CAGR、滚动最差值和连续
OOS 的综合表现最好。入场线在同一 `4/0.85` 基线下比较，`7.5%` 仍为局部
最优。早期实验中使用 `0.95` 隐藏默认值的入场线对照不再作为当前证据。

同轮候选池审计见 `data/round7_pool_audit.json`：210 只均有 K 线和分红缓存，
异常价格 0，动态池月度数量为 78–169（中位数 122）。时间/执行审计见
`data/round7_temporal_audit.json`，相关测试 32 项通过。

## 无未来函数检查

- 交易使用信号日之后的下一交易日精确收盘价；不能成交的停牌日不会用未来价格代替。
- 后置 DPS 只使用除权日不晚于信号日的记录；当前年度尚未实施的分配不会进入信号。
- 分红现金和送转股按真实除权日处理，且除权日必须严格晚于持仓入场日。
- 同一除权日的完全重复接口记录只处理一次；不同报告年度的记录保留。
- 动量回看只访问信号日以前的价格；多周期模式要求每个回看期都有价格。
- manifest 校验每只股票的 K 线、分红明细哈希；日期文件校验 `as_of`、manifest 哈希和日期哈希。

## 已知限制

- 210 只代码是截至 2026-08-25 冻结的当前缓存集合，缺少退市股票和历史成分
  变化，仍有幸存者偏差；这是一份条件可复现回放，不是全市场历史无偏样本外
  研究。后续人工数据质量门禁回放处理了决策相关路径，放行 237 只并得到
  11.29% CAGR / 55.46% 最大回撤，但排除了 106 只分红证据无法闭合的股票，
  仍有数据可得性偏差。不能据此得出“幸存者偏差已消除”或“已精确量化”的
  结论。
- NAV 只在月度执行点标记，最大回撤和波动率不包含月内路径。第 26 轮日频 NAV
  审计确认：日频最大回撤为 32.44%（vs 月频 28.06%），月度口径低估约 4.4pp。
  报告月度指标时应同时标注此参考值；税费对分红再投资采用持仓日期近似，
  未做逐笔先进先出税务模拟。
- 2026 年只到 8 月 25 日；重新刷新缓存、改变数据源或改变日期文件后，必须生成新的 manifest/哈希并重新跑全部实验。
- 41.38% CAGR 是历史样本结果，不能据此承诺未来收益或建议实盘买卖。

第 13 轮严格 warm-up 窗口保留起点前 4 个信号点并按正式起点 NAV 归一化，
会剥离 warm-up 期间及首期建仓费用，不能解释为从窗口起点重新用现金建仓。
第 14 轮价格基准和第 15 轮控制均不含完整分红再投资，且共享冻结现存代码
集合，不能替代无生存偏差的历史总回报指数。
第 17 轮的 30/60/90 天信息延迟是对 `ex_date` 的保守代理；由于缓存没有公告日
或登记日，不能据此估计真实信息发布延迟。第 16 轮稀疏调仓的滚动值按日历跨度
计算，不能与旧的“固定观测数”数字混用。

## 本轮探索记录（2026-08-26）

### 严格连续 OOS 与交叉实验

产物：`data/round5_walkforward.json`、`data/round6_robustness.json`。

- `4/0.85` 的相邻阈值 `0.84/0.86` 仍分别得到 38.07%/38.92% CAGR，滚动 36 月最差约 9%，不是单点孤立峰值。
- `pool_min_consecutive_years=3` 最好；2 年把最大回撤推高到 50.37%，4 年以上收益和滚动稳定性下降。
- 入场线 7.5% 在当前样本的邻域明显占优；这是参数敏感性，后续新数据必须重新验证，不能把它视为稳定常数。
- `lag=2/3`、1/3/4 只持仓、`hold_yield=5.0/6.0` 均不如当前组合。
- 现金保留额 3,000～6,000 元只能略降回撤，未改善最差滚动窗口；固定 5,000 元版本作为低回撤对照保留。

### 账本与元数据修正

- 再投资和等权补仓把手续费计入现金保留额，交易完成后不打穿 reserve；`max_position_pct` 和 reserve 增加范围校验。
- 精确分红先去掉完全重复记录，同时保留同日不同报告年度的分配；补充同日和税期回归测试。
- 实验脚本统一改为完整账本连续切片 OOS，避免重新开账户造成虚假样本外数字。
- manifest 生成器显式写入动态池规则、行情源及 fallback；日期文件重新生成并绑定 manifest 哈希。

## 历史结果状态

README、旧归档和早期实验中出现的 38.95%、40.24%、43.42%、70%～86% 等数字来自旧缓存、复权口径、提前入账、日期缺口或固定精选池，不能与本页当前口径直接比较，也不再称为当前最优。
